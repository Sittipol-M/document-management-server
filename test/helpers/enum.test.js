const { isInEnum } = require("../../helpers/enum");

test("Test isInEnum()", () => {
  // const enumsArray = ["enum1", "enums2"];
  // const enumsNotArray = "notArray";
  // const enumsEmptyArray = [];
  // const checkEnumNumber = 2;
  // const checkEnumBoolean = true;
  // const checkEnumString = "enum";
  // expect(isInEnum({ enums: ENUMS, checkEnum: "enums0" })).toBe(false);
  // expect(isInEnum({ enums: ENUMS, checkEnum: "enum1" })).toBe(true);
  // expect(isInEnum({ enums: ENUMS }));
});
